# Admin agent status UI restoration

Restored the existing private `AdminAgentStatusPanel` integration in `ProfileView` after the independent PWA blank-screen incident was isolated and the service worker was retired.

Scope: admin-only UI visibility for `alejandrorastudini@gmail.com`. The existing server-side authorization remains authoritative.
